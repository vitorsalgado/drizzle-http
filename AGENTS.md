# Agent Instructions

After every change, run these commands in order and fix any failures before finishing:

```bash
npm run fmt
npm test
npm run lint
```

- `npm run fmt` — format the codebase with Prettier
- `npm test` — run the test suite
- `npm run lint` — run ESLint
